[profile.default]
src = "contracts"
out = "foundry-out"
libs = ["node_modules"]
solc_version = "0.8.24"
via_ir = true
optimizer = true
optimizer_runs = 200
fuzz = { runs = 1000 }

[profile.ci]
fuzz = { runs = 5000 }
